import 'package:flutter/material.dart';

class ChipSelelctionCard extends StatelessWidget {
  const ChipSelelctionCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
